========
visaplan
========

This package doesn't provide any special functionality;
it's only purpose is to create the `visaplan` package namespace
to help with problems when e.g. `visaplan.tools` submodules can't be imported.
