from setuptools import setup

setup(name='nic_wanavit_statistics_test',
      version='0.1',
      description='Gaussian distributions',
      packages=['nic_wanavit_statistics_test'],
      zip_safe=False)
