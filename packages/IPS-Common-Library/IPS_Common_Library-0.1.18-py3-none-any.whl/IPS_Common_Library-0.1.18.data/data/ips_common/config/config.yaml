database:
  server: ${DB_SERVER}
  user: ${DB_USER_NAME}
  password: ${DB_PASSWORD}
  database: ${DB_NAME}

logging:
  file:
    file_name: ${LOG_FILE}
    file_level: ${LOG_FILE_LEVEL}

  console:
    console_level: ${LOG_CONSOLE_LEVEL}
