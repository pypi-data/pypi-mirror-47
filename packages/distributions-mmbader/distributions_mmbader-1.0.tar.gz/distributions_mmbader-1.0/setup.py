from setuptools import setup

setup(name='distributions_mmbader',
      version='1.0',
      description='Gaussian distributions',
      packages=['distributions_mmbader'],
      author='Michael Bader',
      zip_safe=False)
