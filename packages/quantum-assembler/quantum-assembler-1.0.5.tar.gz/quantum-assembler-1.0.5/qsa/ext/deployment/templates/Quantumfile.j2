---
deployment:
  # Specifies the environment to which the application may be
  # deployed.
  {%- if spec.deployment.environments %}
  environments:
    {{ spec.deployment.environments|yaml|indent(4) }}
  {%- else %}
  environments: {}
  {%- endif %}
