---
jenkins:
  # Specifies the base folder where items for this project are
  # created. The default is '/'.
  {%- if spec.jenkins.folder %}
  folder: {{ spec.jenkins.folder|default("''") }}
  {%- else %}
  folder: ''
  {%- endif %}
