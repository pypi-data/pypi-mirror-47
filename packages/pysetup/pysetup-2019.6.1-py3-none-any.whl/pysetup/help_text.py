help_text = '''\
Commands list:
    help --- View commands list.
    init --- Create a setup.py file.
    upload <is_clear=False> --- Upload to https://upload.pypi.org/legacy. [is_clear: clear package files before and after upload]
    upload_test <is_clear=False> --- Upload to https://test.pypi.org/legacy. [is_clear: clear package files before and after upload]
    package --- Package the project(create build, dist, *.egg-info file).
    clear --- Clear packaged file(clear build, dist, *.egg-info file).
    install_test <package_name> --- Install the test package from testpypi.
'''
