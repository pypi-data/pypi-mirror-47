[search]
FROM    = alerts@citibank.com
SUBJECT = Transaction Amount Exceeded Alert

[amount+date+description]
fetch   = 1
decode  = ascii
extract = Account #: [X0-9]+ \$(?P<amount>[0-9,.]+) at (?P<description>.+) on (?P<date>.+) exceeds the
